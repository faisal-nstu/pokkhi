{
  "name": "pokkhi",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 3000 --host",
    "build": "vite build",
    "preview": "vite preview --port 3000 --host",
    "lint": "echo 'Vanilla JS app - no lint needed'"
  },
  "devDependencies": {
    "vite": "^5.0.0"
  }
}
